# 🚀 Как запустить OilCalcApp - Подробная инструкция для новичков

## 📋 Что нужно перед началом

1. **Mac компьютер** (iOS приложения можно запускать только на Mac)
2. **Xcode** - бесплатная программа от Apple
   - Если не установлен: открой App Store → найдите "Xcode" → Установить
   - Установка может занять 20-30 минут (программа большая)

---

## 🎯 ШАГ 1: Открой Xcode

1. Нажми на иконку **Finder** (синяя улыбка в Dock)
2. Открой папку **Программы** (Applications)
3. Найдите **Xcode** (синяя иконка с молотком)
4. Дважды кликните, чтобы открыть

> ⚠️ **Первый раз?** Xcode может спросить установить дополнительные компоненты - нажмите **Install** и подождите.

---

## 🎯 ШАГ 2: Создай новый проект

### 2.1 Открой окно создания проекта

1. В Xcode нажми **File** (вверху экрана)
2. Выбери **New → Project** 
   - Или нажми клавиши: `⌘⇧N` (Command + Shift + N)

### 2.2 Выбери тип проекта

1. В левой колонке выбери **iOS**
2. В правой части выбери **App** (первая иконка)
3. Нажми **Next** (внизу справа)

### 2.3 Заполни настройки проекта

Заполни поля:

- **Product Name:** `OilCalcApp`
  - Это название приложения
  
- **Team:** Оставь как есть (или выбери свой Apple ID, если есть)
  
- **Organization Identifier:** `com.yourname` 
  - Замени `yourname` на своё имя (например: `com.ivan`)
  - Это уникальный идентификатор
  
- **Interface:** `SwiftUI` ⚠️ **ВАЖНО!** Должно быть именно SwiftUI
  
- **Language:** `Swift` (должно быть выбрано по умолчанию)
  
- **Storage:** `None` (оставь как есть)

4. Нажми **Next**

### 2.4 Сохрани проект

1. В открывшемся окне найдите папку **Desktop**
2. Выбери папку **OilCalcApp** (та, где лежат файлы проекта)
3. Нажми **Create**

> ⚠️ **Важно:** Xcode спросит: "The folder OilCalcApp already exists. Replace it?"
> - Нажми **Replace** 
> - Это безопасно - заменится только конфигурация проекта, ваши файлы останутся

---

## 🎯 ШАГ 3: Добавь файлы в проект

После создания проекта вы увидите левую панель (навигатор) с файлами.

### 3.1 Удали лишний файл (если есть)

1. В левой панели найдите файл `ContentView.swift` в группе `OilCalcApp`
2. Если он есть - кликните правой кнопкой → **Delete**
3. Выбери **Move to Trash** (отправить в корзину)

> 💡 У нас уже есть свой `ContentView.swift` в папке `OilCalcApp/`, поэтому дубликат не нужен.

### 3.2 Добавь папки с кодом

Теперь нужно добавить папки `Domain`, `Infrastructure` и `Presentation` в проект.

**Способ 1: Перетаскивание (самый простой)**

1. Открой **Finder**
2. Перейди в папку `Desktop/OilCalcApp`
3. Вернитесь в Xcode
4. **Перетащите** из Finder в Xcode:
   - Папку `Domain` → перетащите в группу `OilCalcApp` (в левой панели)
   - Папку `Infrastructure` → перетащите в группу `OilCalcApp`
   - Папку `Presentation` → перетащите в группу `OilCalcApp`

5. При перетаскивании появится окно - настройте:
   - ✅ **Copy items if needed** - **НЕ ставьте галочку** (файлы уже на месте)
   - ✅ **Create groups** - выберите это (кружок должен быть активен)
   - ✅ **Add to targets: OilCalcApp** - должна быть **галочка**
   - Нажми **Finish**

**Способ 2: Через меню (если не получилось перетащить)**

1. В Xcode кликните правой кнопкой на группу `OilCalcApp` (в левой панели)
2. Выберите **Add Files to "OilCalcApp"...**
3. В открывшемся окне найдите папку `Desktop/OilCalcApp`
4. Выберите папки:
   - `Domain`
   - `Infrastructure` 
   - `Presentation`
5. Внизу окна настройте:
   - ✅ **Copy items if needed** - **НЕ ставьте галочку**
   - ✅ **Create groups** - выберите
   - ✅ **Add to targets: OilCalcApp** - должна быть **галочка**
6. Нажми **Add**

### 3.3 Проверь, что файлы добавились

В левой панели Xcode вы должны увидеть:
```
OilCalcApp
├── OilCalcApp
│   ├── OilCalcAppApp.swift
│   ├── ContentView.swift
│   ├── Localizable.strings
│   └── Assets.xcassets
├── Domain
│   ├── BetaTable.swift
│   ├── Calculator.swift
│   └── ... (другие файлы)
├── Infrastructure
│   └── Persistence
│       └── HistoryService.swift
└── Presentation
    ├── Components
    └── Screens
```

---

## 🎯 ШАГ 4: Настрой Deployment Target

Это нужно, чтобы приложение работало на нужной версии iOS.

1. В левой панели кликните на **синюю иконку** в самом верху (это проект)
2. В центральной части выберите **OilCalcApp** под "TARGETS"
3. Откройте вкладку **General** (должна быть открыта по умолчанию)
4. Найдите раздел **Deployment Info**
5. В поле **iOS** выберите `17.0` (или выше)

---

## 🎯 ШАГ 5: Выбери симулятор

Вверху Xcode есть панель инструментов. Слева найдите выпадающий список с названием устройства.

1. Кликните на список (там может быть написано "iPhone 15 Pro" или другое)
2. Выберите любой iPhone симулятор:
   - iPhone 15 Pro (рекомендуется)
   - iPhone 14
   - iPhone SE
   - Или любой другой

> 💡 **Симулятор** - это виртуальный iPhone на вашем Mac, где будет запускаться приложение.

---

## 🎯 ШАГ 6: Запусти приложение! 🚀

1. Нажми большую кнопку **▶️ Play** в левом верхнем углу Xcode
   - Или нажми клавиши: `⌘R` (Command + R)

2. **Первый запуск может занять время:**
   - Xcode скомпилирует проект (соберёт приложение)
   - Запустится симулятор iPhone
   - Приложение откроется на симуляторе

3. **Готово!** 🎉 Вы должны увидеть главный экран приложения с тремя карточками:
   - Базовый калькулятор
   - Анализ потерь
   - История

---

## ❌ Если что-то пошло не так

### Ошибка: "Cannot find type 'DensityMode'"

**Что это значит:** Xcode не видит файл с определением `DensityMode`

**Как исправить:**
1. Убедитесь, что файл `Domain/TripModels.swift` есть в левой панели
2. Кликните на файл правой кнопкой → **Get Info**
3. Проверьте, что в разделе "Target Membership" стоит галочка у **OilCalcApp**
4. Если галочки нет - поставьте её

### Ошибка: "Cannot find 'BetaTable'"

**Как исправить:**
1. Проверьте файл `Domain/BetaTable.swift`
2. Убедитесь, что он добавлен в target "OilCalcApp" (см. выше)

### Ошибка: "Cannot find 'HistoryService'"

**Как исправить:**
1. Проверьте файл `Infrastructure/Persistence/HistoryService.swift`
2. Убедитесь, что он добавлен в target "OilCalcApp"

### Файлы не видны в Xcode

**Как исправить:**
1. Правый клик на группу `OilCalcApp` в левой панели
2. **Add Files to "OilCalcApp"...**
3. Выберите папки `Domain`, `Infrastructure`, `Presentation`
4. Убедитесь, что стоит галочка "Add to targets: OilCalcApp"
5. Нажми **Add**

### Симулятор не запускается

**Как исправить:**
1. Попробуйте выбрать другой симулятор (iPhone 14, iPhone SE)
2. Если не помогает - перезапустите Xcode
3. Проверьте, что у вас достаточно места на диске (нужно ~10 ГБ)

### Приложение не компилируется (много ошибок)

**Как исправить:**
1. В Xcode нажми **Product → Clean Build Folder** (или `⌘⇧K`)
2. Попробуйте запустить снова (`⌘R`)
3. Если не помогло - проверьте, что все файлы добавлены в target (см. выше)

---

## 📱 Что дальше?

После успешного запуска вы можете:

1. **Тестировать приложение** на симуляторе
2. **Изменять код** - изменения можно видеть в реальном времени
3. **Запускать на реальном iPhone** (нужен Apple Developer аккаунт)

---

## 💡 Полезные советы

- **Горячие клавиши:**
  - `⌘R` - Запустить приложение
  - `⌘B` - Собрать проект (без запуска)
  - `⌘.` - Остановить запущенное приложение

- **Если что-то сломалось:**
  - Попробуйте **Product → Clean Build Folder**
  - Закройте и откройте Xcode заново
  - Проверьте, что все файлы на месте

---

**Удачи! Если возникнут вопросы - проверьте раздел "Если что-то пошло не так" выше.** 🎉
