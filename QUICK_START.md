# ⚡ БЫСТРЫЙ СТАРТ - 3 ШАГА

## 🎯 Просто запусти проект!

### Шаг 1: Открой Xcode
Запусти приложение **Xcode** на Mac

### Шаг 2: Создай новый проект
1. **File → New → Project** (или ⌘⇧N)
2. Выбери **iOS → App**
3. Нажми **Next**
4. Заполни:
   - **Product Name:** `OilCalcApp`
   - **Interface:** `SwiftUI` ⚠️ ВАЖНО!
   - **Language:** `Swift`
5. Нажми **Next**
6. **Сохрани в:** `Desktop/OilCalcApp` 
   - ⚠️ **ВАЖНО:** Выбери папку `OilCalcApp` и нажми **Create**
   - Xcode спросит: "The folder OilCalcApp already exists. Replace it?"
   - Нажми **Replace** (это заменит только конфигурацию, файлы останутся)

### Шаг 3: Добавь файлы в проект
1. В Xcode (левая панель) увидишь группу `OilCalcApp`
2. **Удали** автоматически созданный `ContentView.swift` (если есть)
3. **Перетащи** из Finder в Xcode:
   - Папку `Domain` → в группу `OilCalcApp`
   - Папку `Infrastructure` → в группу `OilCalcApp`
   - Папку `Presentation` → в группу `OilCalcApp`
4. При перетаскивании:
   - ✅ **Copy items if needed** - НЕ ставь галочку
   - ✅ **Create groups** - выбери это
   - ✅ **Add to targets: OilCalcApp** - должна быть галочка
   - Нажми **Finish**

### Шаг 4: Настрой Deployment Target
1. Выбери проект (синяя иконка сверху)
2. Выбери Target **OilCalcApp**
3. Вкладка **General**
4. **Deployment Info → iOS:** выбери `17.0`

### Шаг 5: Запусти!
1. Выбери симулятор (например, iPhone 15 Pro)
2. Нажми **▶️ Run** (или ⌘R)
3. Готово! 🎉

---

## ❌ Если что-то не работает:

### Ошибка: "Cannot find type 'DensityMode'"
**Решение:** Убедись, что файл `Domain/TripModels.swift` добавлен в target

### Ошибка: "Cannot find 'BetaTable'"
**Решение:** Убедись, что файл `Domain/BetaTable.swift` добавлен в target

### Ошибка: "Cannot find 'HistoryService'"
**Решение:** Убедись, что файл `Infrastructure/Persistence/HistoryService.swift` добавлен в target

### Файлы не видны в Xcode
**Решение:** 
1. Правый клик на группу `OilCalcApp`
2. **Add Files to "OilCalcApp"...**
3. Выбери папки `Domain`, `Infrastructure`, `Presentation`
4. Убедись, что выбран target "OilCalcApp"
5. Нажми **Add**

---

## 📱 После запуска:

Приложение откроется на симуляторе с главным экраном:
- **Базовый калькулятор** - расчёт массы/литров
- **Анализ потерь** - расчёт потерь при транспортировке
- **История** - все сохранённые расчёты

**Всё готово к использованию!** ✅

